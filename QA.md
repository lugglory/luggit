# 요구사항 대조 기록

> 이 기록은 두 플러그인이 한 저장소의 `obsidian-plugins/` 아래에 함께 있던 시기에 작성되었습니다. 날짜가 붙은 검토 기록은 당시 상태 그대로 남깁니다.

## 사용자 요청

| 요청 | 반영 |
| --- | --- |
| `obsidian-plugin` 브랜치, Git/문서 이동 독립 폴더 | 두 플러그인 각각 번들·매니페스트, Git 스타일 파일 |
| 기존과 비슷한 Git 패널 | 변경/스테이지 목록, diff, 자동 커밋 제목, 최근 파일, Pull/Push. 최근 커밋 영역과 전용 로그 조회는 제거 |
| 커밋 후 Push | 한 버튼에서 순차 실행, Push 실패 시 커밋 보존 및 단독 재시도 |
| 커밋+Push 단일 아이콘 | 위 화살표 아래 가로선을 커밋의 선/원으로 대체 |
| Git 느낌의 플러그인 아이콘 | Git 공식 SVG의 원본 도형 사용, 리본과 탭에 동일 적용 |
| 브랜치/상태 문구 제거 | 패널에서 제거, 진행/결과/오류는 Notice |
| 상단 버튼 가운데 정렬, 그룹 구분 제거, 전체 버리기 대신 언스테이징 | 커밋+Push·커밋을 맨 왼쪽에 두고 스테이지·언스테이징·Push·Pull을 동일 간격으로 배치 |
| 툴팁 중복과 Git 작업 말풍선 제거 | 툴바는 숨긴 이름을 aria-labelledby로 참조, 개별 아이콘·파일명은 Obsidian setTooltip만 사용 |
| 새로고침 버튼을 Pull 버튼으로 교체 | Pull 버튼은 저장→원격이 있으면 Pull→로컬 목록 갱신. 목록은 자동 갱신되므로 새로고침 버튼 제거, 자동 목록 갱신(파일 변경·주기·포커스·마우스 진입)은 로컬 조회만 유지 |
| 한 줄 커밋 입력창 | 내용·줄바꿈·패널 폭에 따라 자동 높이 조절, 삭제·커밋 완료 시 축소 |
| 목록에는 파일명, 비교창에는 경로 | 변경 목록·최근 파일은 파일명과 경로 툴팁, diff 제목은 전체 경로 |
| 최근 파일은 마지막 변경 커밋의 diff | 삭제된 경로도 목록 유지, 클릭한 파일만 조회. 커밋 번호 표시, 현재 파일이 있을 때만 문서 열기 |
| 상단 버튼 기본 무색 배경, 대기 상태는 아이콘 색 | 기본 투명/무테, 대기 아이콘에 강조색, 호버에만 배경 |
| 상태 문자 고정 색상 | M 주황, A 초록, D 빨강 등 테마 변수와 독립 |
| 섹션 제목 옆 버튼 유지 | 스테이지됨: 전체 해제, 변경됨: 전체 버리기·스테이지 |
| 파일별 버튼 기본 투명/무테 | 기본 배경·테두리·그림자 제거, 호버 배경 |
| 파일명 링크 효과 대신 행 강조 | 글자색·밑줄 고정, 행 배경 강조 및 행 클릭으로 diff |
| 얇은 Git 목록 행 | 최소 높이 24px, 세로 여백 0, 행 아이콘 14px, 최근 파일 간격 축소 |
| 영역 간 여백·구분선 제거, 섹션 버튼 통일감 | 구분선 없이 영역 간격 6px, 제목·행 아이콘은 14px로 통일. 제목 작업 버튼은 가로 정렬을 유지하고 사이 여백까지 28×28px 클릭·호버 영역으로 사용 |
| 섹션 접기·펼치기 | 스테이지됨·변경됨·최근 변경한 파일 제목 행으로 토글, 새로고침 후 상태 유지, 전체 작업 버튼은 별도 동작 |
| 섹션 제목 정렬·전체 행 강조·하위 들여쓰기 | 호스트 버튼 가운데 정렬을 덮어쓰는 flex-start, 작업 버튼까지 전체 행 호버, 하위 항목 16px 들여쓰기 |
| 확인창 이름/오른쪽 정렬/Enter·Space | 공통 확인창으로 통일, 승인 포커스, 취소/Esc/키 반복 처리 |
| 종료 시 미커밋 경고 | 로컬 Git 재조회 + 미저장 편집 확인, 동기식 기본 대화상자 |
| 읽기 쉬운 diff | 메타데이터 제외, 줄 번호·추가/삭제 수·색상·표·긴 diff 제한 |
| diff 선택·복사, 제목 행에서 편집 | 본문 선택 허용. 제목 행 클릭/Enter/Space로 편집하되 강조는 글자만 적용하고 X 유지. 하단 제목·변경 내용 복사/문서 열기 버튼. 삭제 파일도 복사 가능, 복사는 표시 줄 수 제한 미적용. Ctrl+Enter 열기 없음 |
| 종료 Push 상태가 Git 변경을 만들지 않음 | 기기별 localStorage에 보관함·설정 폴더별 저장. 일반 설정은 data.json에 유지. Push 성공·실패 시 saveData 호출 없음 |

## 기존 앱과 대조하며 보완한 항목

- 커밋 입력창의 Ctrl/Cmd+Enter.
- 시작 시 Pull, 종료 시 이미 만들어 둔 커밋만 Push, 실패 시 다음 실행 안내.
- 이름 변경의 양쪽 경로 스테이지 해제, 첫 커밋 전 스테이지 해제, 인덱스 잠금 오류 시 보존.

## 검증 범위와 차이

- Git 명령은 임시 실제 저장소와 로컬 bare 원격으로 검증합니다. 사용자 보관함/원격에는 테스트 데이터를 쓰지 않습니다.
- UI는 실제 Electron/DOM과 Obsidian API 대역으로 검증합니다. **실제 Obsidian 호스트에서 검증했다는 뜻은 아닙니다.**
- 종료 취소는 Electron `beforeunload` 경로에서 검증합니다. Obsidian 호스트의 종료 방식, 앱 업데이트, 강제 종료에 따라 경고·종료 Push가 생략될 수 있습니다.
- Pull은 깨끗한 작업 폴더에서 `--ff-only`로 제한합니다. 기존 앱의 자동 merge와 다릅니다.
- 파일 저장·일반 Undo·복구 이력은 Obsidian에 맡깁니다. 기존 Electron 앱의 파일 저장 엔진과 복구 보관함은 플러그인 범위에 포함하지 않습니다.

## 2026-09-18 소스·실행 파일 대조

- 기준: 대화의 최신 수정 요청, 최초 플러그인 커밋 `0a1ae60`, 현재 `obsidian-plugins/` 작업 트리. 위 요구사항 표를 실제 소스·스타일·매니페스트와 대조했습니다.
- `0a1ae60` 이후 코드 차이는 요청한 목록 행 축소, 입력창 자동 높이, 커밋+Push·커밋 우선 배치, 탭 이동 시 아래 패널 생성 및 원래 탭 닫기에 해당합니다. 그 이전에 반영한 버튼·색상·diff·확인창·종료 경고·순환 이동 기능도 유지되어 있습니다. 현재 파일에서 요청과 무관한 삭제나 이전 구현으로의 복귀는 발견하지 못했습니다.
- 기존 `main.js`를 덮어쓰지 않고 `build.mjs`와 같은 옵션으로 메모리에만 빌드했습니다. 두 플러그인 모두 기존 번들과 생성 결과가 바이트 단위 및 SHA-256 비교에서 완전히 일치했습니다. 소스만 최신이고 실행용 번들이 과거 버전인 상태는 아닙니다.
- 전체 자동 테스트 35개 통과. 재빌드하지 않은 기존 번들로 Electron UI 검증 통과: 버튼 순서·섹션 버튼 유지·기본 투명 배경·아이콘 강조·상태 색상·입력창 확장/축소·패널 생성·탭 이동 후 원래 탭 닫기·확인창·diff·종료 취소.
- 이번 검토로 런타임 코드를 추가 수정하지 않았습니다. 최신 수정사항과 이 검토 기록을 함께 커밋합니다.
- 범위 제한: 별도 보관함에 복사한 설치본, 실제 Obsidian 호스트 및 기록되지 않은 과거 복사 작업은 이번 대조에 포함하지 않았습니다. 위에 기록한 기존 이식 범위의 차이도 그대로입니다.
